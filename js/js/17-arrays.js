// Arrays
const myArray = [];

// add elements to an array
myArray[0] = "Mesa";
myArray[1] = 1001;
myArray[2] = false;

// method in array to used
myArray.push("ITC");

// refer to an array
console.log(myArray)

// length property
console.log(myArray.length)

// last element in an array
console.log(myArray[myArray.length - 1]);